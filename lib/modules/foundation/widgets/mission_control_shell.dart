import 'package:flutter/material.dart';

class MissionControlShell extends StatelessWidget {
  final Widget child;

  const MissionControlShell({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
