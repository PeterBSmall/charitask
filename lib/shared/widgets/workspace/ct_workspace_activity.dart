import 'package:flutter/material.dart';

class CTWorkspaceSection extends StatelessWidget {
  const CTWorkspaceSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
